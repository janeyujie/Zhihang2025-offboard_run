<center>
    <h1>项目总结报告</h1>
</center>

## 项目概述

本项目旨在开发一套运用智能算法实现自主任务执行的虚拟无人机系统。系统通过集成 GPS、计算机视觉等技术，使无人机具备在复杂环境中进行高低空目标识别与追踪功能。任务流程分为两个阶段：第一阶段由垂起固定翼无人机 A 执行侦察任务，负责在指定区域内完成模式切换、避障及目标识别定位，并向四旋翼无人机 B 发送三名模拟人员的精确坐标；第二阶段由四旋翼无人机 B 执行精准救援任务，在无人机 A 降落后起飞，对指定目标进行路径规划与精准追踪，最终在特定高度发布精确的降落平台坐标。

## 项目结构

项目采用 ROS1（机器人操作系统）作为核心开发框架，结构设计清晰合理，文件结构组织合理，便于模块化开发与维护。`src`目录下的 `offboard_run`功能包包含核心节点：其中 `vtol_commander.py`和 `detect_stage1.py`负责第一阶段任务逻辑；`iris_commander.py`、`detect_red.py`和 `detect_white.py`则负责第二阶段的精确定位与追踪逻辑。

## 开发过程

### 阶段一

在第一阶段，我们实现了无人机 A 的基础飞行控制功能，包括路径规划与避障。通过 `vtol_commander.py`，无人机 A 能够接收目标区域位置信息，规划避障路径，并在接近目标点时发布到达信号。随后激活目标识别模块 `detect_stage1.py`，对遥感数据进行实时处理，计算并发布目标物的精确坐标。

### 阶段二

在第二阶段，`iris_commander.py`依据第一阶段提供的目标坐标，控制无人机 B 飞抵目标上空进行递进式侦查。`detect_red.py`与 `detect_white.py`分别执行对红色危重目标与白色健康目标的抵近识别任务。

## 项目分工

项目分工明确，成员各司其职负责特定模块的开发，有效确保了开发效率与代码质量。合理的任务分配与高效的工具应用，保障了项目快速推进、及时同步，以及各模块的独立性与系统集成的顺畅性。

## 技术亮点

### 一键启动与自动化计分脚本

通过 `launch.sh`脚本实现了一键启动仿真环境的功能，配合 `score_check.py`对仿真结果进行自动化计分，显著简化了测试与调试流程，大幅提升了开发效率。

### YOLOv8 模型集成与遥感数据实时处理

在 `detect_stage1.py`、`detect_red.py`和 `detect_white.py`中，集成了 YOLOv8 模型进行高精度目标检测。通过对预训练模型进行定制化数据训练，获得了良好的目标识别效果。同时，利用多线程与消息队列技术，实现了遥感数据的实时同步处理。在定位算法层面，基于视线模型构建了遥感目标的精确定位机制。

### 路径规划与目标追踪控制

。。。

### 高低空 UAV 协同智能体

。。。

## 总结

本项目成功实现了无人机高低空协同目标识别与追踪系统。通过两阶段开发，系统能够可靠完成自动导航、高空目标识别定位以及低空目标精确追踪等任务。项目模块划分清晰合理，技术方案具备先进性。未来工作中，我们将持续优化代码架构，进一步提升系统的稳定性、可靠性与执行效率，以应对更复杂的实际应用场景需求。