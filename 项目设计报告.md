<div style="text-align: center;">
<font size="6">第十九届“挑战杯”全国大学生课外学术科技作品竞赛“揭榜挂帅”专项赛</font>

# 选题名称
## 作品名称
xxxxx
## 学校
北京航空航天大学
## 队名
xxxx
## 成员
郑宇杰、传真奇、张家文、杨佳宇轩

## 目录
- [1. 项目简介](#1-项目简介)
- [2. 项目背景](#2-项目背景)
- [3. 项目目标](#3-项目目标)
- [4. 项目实施](#4-项目实施)
  - [4.1 阶段一](#41-阶段一)
  - [4.2 阶段二](#42-阶段二)
  - [4.3 阶段三](#43-阶段三)
- [5. 项目结构](#5-项目结构)
- [6. 开发过程](#6-开发过程)
- [7. 文件说明](#7-文件说明)
- [8. 关键代码及注释](#8-关键代码及注释)
- [9. 运行须知](#9-运行须知)

[TOC]



## 1. 项目简介
本项目旨在开发一款利用智能算法操控自主完成任务的虚拟无人机系统，通过使用雷达、GPS和计算机视觉等技术，实现无人机在复杂环境中的海上自主飞行和救援任务。整个任务分为两个阶段：第一阶段由垂起固定翼无人机A执行侦察任务，负责在指定区域内进行模式切换、避障和目标识别，并向四旋翼无人机B发送三名模拟人员的坐标；第二阶段由四旋翼无人机B执行精准救援任务，在无人机A降落后起飞，对指定目标进行路径规划和精准追踪，最终在特定高度发布精确的降落平台坐标。
## 2. 项目背景
随着无人机技术的快速发展，其在自主巡检、环境监测和复杂任务执行等领域的应用日益广泛。然而，在包含禁飞区、多种目标识别和多机协作等复杂场景下，如何实现无人机的高效自主导航、智能避障和精准任务分配，仍然是一个亟待解决的难题。

本项目旨在应对这类挑战，提出了一种综合性的解决方案，开发了一套基于智能算法的虚拟无人机系统。该系统在一个包含居民区禁飞区和多种目标物的2千米x2千米仿真地图中，通过多无人机协同完成任务。该任务要求无人机A（垂起固定翼）在避开禁飞区的前提下，自主切换飞行模式，并在指定区域内准确搜索并识别不同状态的模拟人员（危重、轻伤、健康）。随后，无人机B（四旋翼）需根据无人机A提供的目标坐标，在避障的同时进行路径规划，并对指定目标（危重、健康）进行精准追踪，最终完成信息发布。本项目的开发，正是为了在模拟环境中验证和提升无人机在复杂任务下的自主决策和协同执行能力。
## 3. 项目目标
本项目通过智能算法操控虚拟无人机自主完成任务。无人机从起飞平台起飞并飞抵GPS引导点，随后利用带有噪声的航向信息寻找目标。在借助视觉引导锁定目标后，无人机将完成降落，结束整个任务过程
## 4. 项目实施
### 4.1 阶段一
### 4.2 阶段二
## 5. 项目结构
### 5.1 图示

> [!NOTE]
>
> 目录结构最后把多余的文件给删除掉

```
├── models
│   ├── stage1.pt
│   ├── stage2.pt
│   ├── white.pt
├── msg
│   ├── Object3DStamped.msg
├── scripts
│   ├── _pycache_
│       ├──commander.cpython-38.pyc
│       ├──iris_command.cpython-38.pyc
│       ├──offboard_run.cpython-38.pyc
│       ├──vtol_commander.cpython-38.pyc
│   ├── commander.py
│   ├── detect_red.py
│   ├── detect_stage1.py
│   ├── detect_white.py
│   ├── iris_commander.py
│   ├── iris_commander1.py
│   ├── iris_test.py
│   ├── launch.py
│   ├── offboard_run.py
│   ├── save.py
│   ├── vtol_commander.py
├── src
│   ├── offboard_run1.cpp
│   ├── offboard_run2.cpp
│   ├── offboard_run3.cpp
├── .gitignore
├── CMakeLists.txt
├── README.md
├── package.xml
├── score_check.py
└── vtol_pose_offset.png
```
### 5.2 部分介绍
## 6. 开发过程
### 6.1 整体流程
### 6.2 具体实施
## 7. 文件说明
### 7.1 detect_stage1.py & vtol_commander.py（第一阶段）

`vtol_commander.py` 根据GPS坐标点绕过禁飞区到达目标区域上空，激活第一阶段的视觉识别进程并开始扫描目标

`detect_stage1.py` 根据vtol的机载遥感来识别目标并实时计算目标坐标并发送

### 7.2 detect_red.py & detect_white.py & iris_commander.py（第二阶段）


## 8. 关键代码及注释
### 8.1 detect_stage1.py代码及注释

`detect_stage1.py` 文件实现了利用vtol无人机遥感数据进行目标识别并定位，并根据不同的目标类型对数据进行特定处理

- 数据读取：使用消息队列保存同一时刻的图像和位置信息避免数据阻塞，由识别线程统一处理
- 识别功能：利用YOLOv8这类预训练模型对遥感数据进行目标识别和标记
- 坐标计算：首先通过可信度筛选出高可信度的标记，通过建立的遥感坐标转换模型来计算物体的具体坐标
- 数据处理：对静止目标坐标进行均值滤波后进行发布，提高位置可信度；对运动目标则选择靠近遥感图像中心位置的坐标进行发布，以减少计算误差

> PS: 遥感坐标转换模型。由像素坐标得到目标相对于相机系的方向向量，然后再由相机系转为机体系，机体系转为世界系坐标，根据地面高度为0的假设和线面相交的几何关系最终计算出目标位置。因为是在运动的平台上进行遥感计算，同时还需要修正数据因采样时间差造成的误差
>
> 1. 像素坐标转相机方向向量，其中$K$为相机内参，$u,v$为目标像素坐标
>
> $$
> \mathbf{p}_{\rm cam}  = \,    K^{-1} \begin{bmatrix} u \\ v \\ 1 \end{bmatrix}  = \begin{bmatrix}      (u - c_x) / f_x \\      (v - c_y) / f_y \\      1    \end{bmatrix}.
> $$
>
> 2. 相机坐标系到机体坐标系，$\mathbf{t}_{\rm cam\to body}$为相对偏移
>    $$
>    \mathbf{p}_{\rm body}  = R_{\rm cam\to body}\,\mathbf{p}_{\rm cam} + \mathbf{t}_{\rm cam\to body}.
>    $$
>    
>
> 3. 机体坐标系到世界坐标系
>    $$
>    \mathbf{p}_{\rm target}  = \mathbf{p}_{\rm UAV}  + R_{\rm body\to world}\,\mathbf{p}_{\rm body}.
>    $$
>    
>
> 4. 三维坐标计算，计算 `ray` 与地面 `Z=ground_z` 的交点 `t= -z/dz`
>    $$
>    t = (ground_z - cam_z)/ray_{world}[2]\\
>    P=Cam_{position}+t*ray_{world}
>    $$
>    
>
> 5. 误差修正，计算误差 $v_{error}$ 并修正，得最终坐标
>    $$
>    P=P+v_{error}
>    $$
>    

```python
# detect_stage1.py
```

### 8.2 vtol_commander.py代码及注释

```python
```



### 8.3 detect_red.py 代码及注释

 iris 到达重伤病员上空后启动该脚本进行目标靠近侦查。视觉识别及坐标计算同第一阶段，主要新增对iris的递进控制

- 控制工作线程，实现“对准-下降”精确降落控制
- 工作状态为一个状态机：在搜索目标，水平对齐，垂直下降之间转换以实现精确降落

```python
# detect_red.py
```



### 8.4 detect_white.py 代码及注释

iris 到达健康人员上空之后进行递进侦查。视觉识别与坐标计算同上，控制逻辑为等待运动目标出现并精确降落。

```python
# detect_white.py
```

### 8.5 iris_commander.py代码及注释

```python
```



## 9. 运行须知
### 9.1 启动方式
### 9.2 依赖项

确保已安装以下依赖项：

#### 1. OpenCV

版本建议'4.10.0' 

可以通过以下命令安装 OpenCV：

```
sudo apt-get install python3-opencv
```

#### 2. YOLOv8  

您需要安装 ultralytics 包来使用 YOLOv8 模型： 

```
pip install ultralytics
```

#### 3. simple_pid  

可以通过以下命令安装 simple_pid 包： 

```          
pip install simple-pid
```

#### 4. cv_bridge  

请确保已安装 cv_bridge 以便将 ROS 图像消息转换为 OpenCV 格式：

```
sudo apt-get install ros--cv-bridge
```

#### 5. gazebo_msgs  

请确保已安装 gazebo_msgs 以便与 Gazebo 进行通信：   

```
sudo apt-get install ros--gazebo-msgs
```

