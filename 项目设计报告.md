<div style="text-align: center;">
    <font size="6">第十九届“挑战杯”全国大学生课外学术科技作品竞赛“揭榜挂帅”专项赛	</font><br><br>
    <font size="6">项目设计报告</font><br><br>
    <font size="5">选题名称</font><br><br>
    <font size="4">SH-10 无人机双机协同自主搜救任务</font><br><br><br>
    <font size="5">作品名称</font><br><br>
    <font size="4">面向自主搜救任务的双机系统智能决策算法研究</font><br><br><br>
    <font size="5">学校</font><br><br>
    <font size="4">北京航空航天大学</font><br><br><br>
    <font size="5">成员</font><br><br>
    <font size="4">郑宇杰、传真奇、张家文、杨佳宇轩</font><br>
</div>


​    

## 目录

[TOC]



## 1. 项目简介
​	本项目聚焦于“无人机双机协同自主搜救”这一前沿挑战，设计并实现了一套面向复杂动态环境的异构无人机协同智能决策系统。系统由一架垂起固定翼无人机（VTOL）作为广域侦察单元和一架四旋翼无人机作为精准响应单元构成。

​	任务流程分为两个核心阶段：首先，VTOL无人机利用其航程远、效率高的优势，在大范围内执行自主航路规划、动态模式切换、躲避禁飞区及多目标实时感知任务，通过搭载的YOLOv8视觉感知模块，实现对三类模拟人员的精确识别与世界坐标解算。在完成侦察并回传目标数据后，四旋翼无人机接替任务，基于接收到的目标信息执行高精度路径规划与末端视觉伺服控制，分别对静态及动态目标进行稳定追踪与抵近，最终在要求的高度上，完成关键信息的精确发布，全过程无需人工干预。


## 2. 项目背景
​	无人机技术在环境监测、应急通信和抢险救灾等领域的应用正迅速深化。尤其在灾后搜救等复杂场景中，地面人员往往面临视野受限、地形阻碍及二次坍塌等重大安全风险。无人机凭借其空中优势，能够快速获取全局态势，显著提升搜救效率并降低人员风险。

​	然而，单一无人机平台难以兼顾大范围搜索效率与近距离精准操作的需求。为此，**异构双机协同**已成为解决此类复杂任务的前沿范式 。该模式利用VTOL无人机兼具多旋翼垂直起降灵活性与固定翼长航时巡航能力的特点，执行广域快速扫描；同时利用四旋翼无人机优越的悬停与机动性能，执行抵近侦察与精准任务操作。本项目正是在此背景下，针对包含禁飞区、多类别动态/静态目标识别以及双机任务交接的复杂协同搜救场景，研发一套高效、鲁棒的智能决策与控制算法，旨在验证并提升异构无人机集群在该类任务中的自主协同作业能力。



## 3. 项目目标
​	本项目的核心目标是设计、实现并验证一套应用于异构双机协同搜救任务的**智能决策与控制算法**。该系统旨在模拟真实搜救场景，验证无人机在GPS信号存在噪声、目标部分动态的挑战下，从起飞到任务完成的全流程自主作业能力。

具体子目标分解如下：

1. **高层任务规划与导航**：实现 VTOL 无人机的全自主起降、飞行模式无缝切换，以及融合了禁飞区的全局路径规划与航点跟踪能力。
2. **实时环境感知与目标定位**：开发基于 YOLOv8 的机载视觉感知模块，实现对多类别目标的实时检测、分类，并构建从像素坐标到全局地理坐标的精确解算模型。
3. **动态目标追踪与运动预测**：针对动态目标，设计一套轨迹数据采样与运动模型拟合算法，实现对目标未来位置的有效预测，为精准拦截提供决策依据。
4. **末端精准伺服与信息发布**：为四旋翼无人机开发基于视觉反馈的闭环伺服控制算法，实现对目标的稳定抵近、对心悬停，并在满足竞赛规则的高度与精度要求下，完成关键信息的发布。



## 4. 项目实施
### 4.1 阶段一：任务启动与广域巡航

​	无人机A (`standard_vtol_0`) 执行任务初始化序列。首先以多旋翼模式垂直起飞至预定安全高度，随后执行**多旋翼至固定翼的飞行模式转换**。进入固定翼模式后，激活全局路径规划模块，结合禁飞区约束，生成一条绕开禁飞区的无碰撞航路，最终飞抵指定的GPS引导点。

### 4.2 阶段二：目标搜索、识别与信息共享

抵达GPS引导点后，无人机A进入搜索模式，开始执行**预设的覆盖式路径规划航线以确保对指定搜索区域的完全覆盖**。在此期间，**机载的视觉感知模块**被激活，该模块基于YOLOv8深度学习模型，对视频流进行实时处理，以检测和分类三类目标（危重、轻伤、健康）。一旦检测到目标，系统将通过坐标解算算法获取其全局Pose，并通过ROS话题向外发布，实现与无人机B的机间数据链模拟传输。完成所有目标的搜索与信息发布后，无人机A自主返航至旋翼模式区，执行固定翼至多旋翼的模式转换，并最终垂直降落、自动锁桨。

### 4.3 阶段三：精准抵近与末端作业

无人机A安全着陆后，无人机B (`iris_0`) 自动解锁起飞。任务规划模块根据接收到的目标信息进行决策，首先规划前往健康人员（动态目标）的航路。抵达目标区域后，激活**末端视觉引导系统**：

- 对于**健康人员：**进入目标状态估计阶段。通过连续的视觉检测，对目标的运动轨迹进行数据采样与拟合，建立其运动模型。基于该模型，计算出最优拦截点并前出至该点上空，最终下降至0.7米高度等待目标进入指定区域后发布其平台坐标。
- 对于**重伤人员**：激活视觉伺服控制逻辑。采用“对准-下降”的递进策略，通过闭环控制不断修正无人机水平位置，确保其精确位于目标正上方，并逐步下降至0.5米高度发布其平台坐标。

### 4.4 阶段四：任务完成与返航

无人机B在完成对两个指定目标的任务后，执行返航程序，自主飞行返回旋翼模式区，并执行自动降落、锁桨，标志着整个协同搜救任务的圆满完成。



## 5. 项目结构
### 5.1 图示

**项目的所有文件位于工作区catkin_ws/src/offboard_run下**

```bash
.
├── CMakeLists.txt
├── README.md
├── models
│   ├── red.pt
│   ├── stage1.pt
│   ├── stage2.pt
│   └── white.pt
├── msg
├── package.xml
├── score_check.py
├── scripts
│   ├── __pycache__
│   ├── detect_red.py
│   ├── detect_stage1.py
│   ├── detect_white.py
│   ├── iris_commander.py
│   ├── launch.sh
│   └── vtol_commander.py
└── src
```
### 5.2 部分介绍

#### src/offboard_run

`offboard_run` 目录包含了主要的 ROS 包和相关的代码文件。该目录下的内容用于实现无人机的导航和控制功能。

- **scripts: **该目录包含Python脚本：
  - `vtol_commander.py`: 该脚本用于固定翼无人机 A 阶段一和阶段二的起飞、搜索巡航、返回并降落的控制，通过ROS实现。 
  - `detect_stage1.py`: 该脚本用于固定翼无人机 A 阶段二的视觉识别，使用YOLOv8进行目标识别与坐标转换，将目标人物的坐标发送到指定话题。
  - `iris_commander.py`: 该脚本用于四旋翼无人机 B 阶段三和阶段四的起飞，接近健康人员目标、接近重伤人员目标、返回并降落的控制，通过ROS实现。
  - `detect_white.py`: 该脚本用于四旋翼无人机 B 阶段三的健康人员的视觉识别和精确降落控制，在接近健康人员目标以后，通过视觉识别的结果进行轨迹采样并预测，得到降落平台的坐标后，下降到指定高度并发布话题。
  - `detect_red.py`: 该脚本用于四旋翼无人机 B 阶段三的重伤人员的视觉识别和精确降落控制，在接近重伤人员目标以后，通过视觉识别的结果进行目标接近与降落，下降到指定高度并发布话题。
- **CMakeLists.txt:** 用于配置项目的CMake构建系统，该文件列出了构建项目所需的源文件和依赖项。
- **package.xml:** 描述ROS包的元数据文件。该文件包含包的名称、版本、维护者、依赖项等信息。
- **models:** 该目录包含了 `YOLOv8` 模型的相关模型文件，用于目标检测。
- **launch.sh:** 该脚本用于一键启动整个项目。
- **score_check.py: **该脚本用于模拟评分。

## 6. 开发过程
### 6.1 整体流程

​	开始项目的开发以前我们先配置了环境，并学习了ros的相关语法知识。为了了解项目情况我们先初步尝试了无人机的飞行，在网络上搜索相关的控制脚本，首先成功实现了无人机解锁，并飞到20米的功能。在了解项目大致内容和目标后，我们将固定翼无人机的功能分为导航控制和视觉识别两个模块，将四旋翼无人机的功能分为导航控制和精确降落两个模块，接着完成两个无人机四个阶段的话题衔接。

### 6.2 项目分工

​	我们的项目按照任务类型——无人机飞行控制与视觉识别两个大方向进行划分，各自由两名同学负责两个模块的开发与测试，最后整合。

- 阶段一：固定翼无人机起飞，飞行控制，避障与搜索路径规划；
- 阶段二：视觉识别，坐标转换；视觉模型的数据采集与训练；
- 阶段三：四旋翼无人机起飞，飞行控制，避障路径规划，接近目标；
- 阶段四：根据视觉识别返回的物体坐标，实现精确降落；视觉模型的数据采集与训练。



## 7. 文件说明
### 7.1  vtol_commander.py（阶段一）

`vtol_commander.py` 用于固定翼无人机的导航控制，根据GPS坐标点绕过禁飞区到达目标区域上空，激活第一阶段的视觉识别进程并进行目标搜索。

#### 7.1.1 整体路线预览

![3](C:\Users\Lenovo\Desktop\offboard_run\resources\3.png)

#### 7.1.2 起飞降落逻辑

对于固定翼无人机，先使用多旋翼模式起飞到20米高度，后切换为固定翼模式完成所有任务，最后回到旋翼模式区时切换回多旋翼模式并降落并锁桨。

![4](C:\Users\Lenovo\Desktop\offboard_run\resources\4.png)

![5](C:\Users\Lenovo\Desktop\offboard_run\resources\5.png)

#### 7.1.3 避障策略

在搜索任务中存在禁飞区，采取的方式是当无人机经过时保持一定距离地绕开；考虑到有房屋和树木的阻挡，在飞行过程中设置了较高的飞行速度避免碰撞，示意图如下。

![1](C:\Users\Lenovo\Desktop\offboard_run\resources\1.png)

### 7.2 detect_stage1.py（阶段二）

`detect_stage1.py` 文件实现了利用vtol无人机遥感数据进行目标识别并定位，并根据不同的目标类型对数据进行特定处理

- 数据读取：使用消息队列保存同一时刻的图像和位置信息避免数据阻塞，由识别线程统一处理
- 识别功能：利用YOLOv8这类预训练模型对遥感数据进行目标识别和标记
- 坐标计算：首先通过可信度筛选出高可信度的标记，通过建立的遥感坐标转换模型来计算物体的具体坐标
- 数据处理：对静止目标坐标进行均值滤波后进行发布，提高位置可信度；对运动目标则选择靠近遥感图像中心位置的坐标进行发布，以减少计算误差

> PS: 遥感坐标转换模型。由像素坐标得到目标相对于相机系的方向向量，然后再由相机系转为机体系，机体系转为世界系坐标，根据地面高度为0的假设和线面相交的几何关系最终计算出目标位置。因为是在运动的平台上进行遥感计算，同时还需要修正数据因采样时间差造成的误差
>
> 1. 像素坐标转相机方向向量，其中$K$为相机内参，$u,v$为目标像素坐标
>
> $$
> \mathbf{p}_{\rm cam}  = \,    K^{-1} \begin{bmatrix} u \\ v \\ 1 \end{bmatrix}  = \begin{bmatrix}      (u - c_x) / f_x \\      (v - c_y) / f_y \\      1    \end{bmatrix}.
> $$
>
> 2. 相机坐标系到机体坐标系，$\mathbf{t}_{\rm cam\to body}$为相对偏移
>    $$
>    \mathbf{p}_{\rm body}  = R_{\rm cam\to body}\,\mathbf{p}_{\rm cam} + \mathbf{t}_{\rm cam\to body}.
>    $$
>
>
> 3. 机体坐标系到世界坐标系
>    $$
>    \mathbf{p}_{\rm target}  = \mathbf{p}_{\rm UAV}  + R_{\rm body\to world}\,\mathbf{p}_{\rm body}.
>    $$
>
>
> 4. 三维坐标计算，计算 `ray` 与地面 `Z=ground_z` 的交点 `t= -z/dz`
>    $$
>    t = (ground_z - cam_z)/ray_{world}[2]\\
>    P=Cam_{position}+t*ray_{world}
>    $$
>
>
> 5. 误差修正，计算误差 $v_{error}$ 并修正，得最终坐标
>    $$
>    P=P+v_{error}
>    $$



### 7.3  iris_commander.py（阶段三）

`iris_commander.py` 用于四旋翼无人机的导航控制，根据阶段二识别到的坐标位置绕过禁飞区到达目标区域上空，激活第四阶段的视觉识别进程并进行精确定位与降落。

#### 7.3.1 整体路线预览

**由于目标人物的生成位置是随机的，此处路线仅为示例**

![6](C:\Users\Lenovo\Desktop\offboard_run\resources\6.png)

#### 7.3.2 控制逻辑

​	对于四旋翼无人机只需要直接使用ROS话题控制无人机飞行即可，不再需要模式切换且可以自由悬停移动，可以更加灵活地控制以实现精确降落。

#### 7.3.3 避障策略

采用相同的逻辑，示意图如下：

![2](C:\Users\Lenovo\Desktop\offboard_run\resources\2.png)

### 7.4 detect_white.py & detect_red.py（阶段四）

​	作用是无人机到达目标区域后进行递进侦查，整体框架分为目标识别进程和控制线程

- 目标识别线程：
  - 同时保存遥感图像和飞机位姿信息于队列中，利用YOLOv8类的视觉识别模型进行目标标注，并缩小识别范围以提高精确度，在选取合理可信度的标注信息通过遥感视线模型计算目标位置，交由无人机控制线程对目标进行递进侦查。因为飞机与目标都在做运行，坐标计算后需要进行相对误差修正，同时还需要控制数据的实时同步处理与发布。
- UAV控制线程：
  - 对于移动中的健康人员：采用搜索—建模—降落—等待的控制逻辑
    - **SEARCHING:** 首先移动到阶段二中搜索得到的坐标位置，并下降到一定高度；
    - **MODELING:** 接着根据视觉识别线程得到的坐标信息开始采样，建模拟合目标人物的运动轨迹；
    - **INTERCEPTING:** 根据计算得到的运动轨迹选择一个拦截点，无人机移动到这个点的上方
    - **DESCENDING_TO_WAIT:** 下降到发布话题的要求高度0.7米
    - **WAITING_FOR_TARGET:** 等待目标通过即发布坐标
  - 对于重伤人员：采用识别——对准——下降的控制逻辑
    - **SEARCHING:** 根据视觉识别算法得到的目标坐标进行水平位置的微调，当无人机恰好移动到目标正上方时进入下一状态；
    - **ALIGNING:** 对准后允许下降一段距离
    - 重复上述过程，直到无人机下降到发布话题的要求高度0.5米



## 8. 关键代码及注释

> 详情见对应的脚本

## 9.创新点

### 9.1 异构双机协同任务规划与GNC一体化设计

​	本项目构建了一套高效的**异构无人机（VTOL与四旋翼）**协同架构。我们设计了一个清晰的任务分配与信息流交接机制：由VTOL执行广域侦察与目标初步定位，通过ROS话题链将高价值目标信息传递给四旋翼，由后者执行高精度抵近与末端作业。

​	在控制层面，我们实现了**任务规划与GNC（制导、导航与控制）系统**的一体化设计。`vtol_commander\iris_commander`脚本中的状态机作为任务规划层，负责顶层任务的逻辑决策与流程切换；它通过MAVROS接口，将决策结果转化为对PX4飞控的位置或速度级指令，形成了完整的自主控制闭环。特别地，在路径规划中，我们成功融入了对禁飞区的规避策略，确保了无人机在复杂环境下的任务合规性与飞行安全。

### 9.2 基于深度学习的多模态视觉感知与精确定位

​	在 `detect_stage1.py`、`detect_red.py`和 `detect_white.py`中，系统集成了**YOLOv8深度学习模型**作为核心视觉感知算法，通过在自定义数据集上进行微调训练，实现了对搜救场景下多类别（危重、轻伤、健康）目标的**高精度实时检测与分类**。

> 训练数据详情请见 [roboflow](https://app.roboflow.com/join/eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ3b3Jrc3BhY2VJZCI6IlQ4VGZpZ0FqQkdOMWIzT1pITjNNMUJ3dDNoazIiLCJyb2xlIjoib3duZXIiLCJpbnZpdGVyIjoiaGFpa2U4MjZAZ21haWwuY29tIiwiaWF0IjoxNzUzNjc5NTUzfQ.ksRN2pjtnuXkUIybOxv0PoRmLFUzQssGo_Zjj3tStE0) 平台，

​	在目标定位方面，我们构建了鲁棒的坐标解算流程。该流程基于**相机针孔模型**，将目标在二维像素平面的坐标，通过相机内参、无人机姿态和相机-机体相对位姿变换，精确地反投影到三维世界坐标系。为解决运动平台下“所见非所得”的时延问题，我们采用`message_filters`对**图像帧、相机信息和无人机位姿进行严格的时空同步**，确保了用于坐标解算的每一组数据都具有高保真度。

### 9.3 路径规划与目标追踪控制：动/静态目标的末端视觉伺服与轨迹预测

针对第二阶段的两种不同目标，我们设计并实现了两种差异化的抵近与追踪策略：

- **静态目标（危重人员）**：我们采用了一种**基于位置的视觉伺服控制方案。无人机以目标的世界坐标为引导，通过高频次的视觉反馈循环，不断计算并修正自身与目标的水平误差，实现了平滑、精准的“对准-下降”复合式降落，保证了最终发布的坐标精度。
- **动态目标（健康人员）**：针对其运动特性，我们设计了一套**“在线建模-预测-拦截”**的智能决策算法。无人机首先悬停于观测点，对目标的时序轨迹数据进行采集；然后采用最小二乘法拟合出目标的圆形运动模型；最后，基于该模型**预测**出目标在未来穿越拦截路径的时刻与位置，并驱动无人机前出至最佳拦截点上空进行等待，极大地提升了任务成功率。

### 9.4 高效的任务开发与自动化验证流程

​	为应对多节点、多模块的复杂ROS系统开发，我们编写了`launch.sh`脚本，实现了从Gazebo仿真环境、PX4飞控到所有控制与识别节点的**一键化部署与启动**，极大地简化了调试流程。同时，我们独立开发了`score_check.py`自动化评分脚本，它通过订阅关键ROS话题并与Gazebo真值进行比对，能够对每次任务执行的最终得分进行**量化评估**。这套高效的“一键启动+自动评分”工作流，为算法的快速迭代、参数优化和性能验证提供了有力支持，是保障项目成功的工程化实践。

## 10. 运行须知
### 10.1 启动方式

要启动该项目，请运行与当前文件同级目录下的 `launch.sh` 脚本，该脚本将启动所有必要的节点和服务，确保无人机能够正确执行路径点导航和目标识别任务。

```bash
# 启动地面站
cd ~/Downloads # 你安装的地面站位置
./QGroundControl.AppImage
# 激活启动脚本
cd ~/catkin_ws/src/offboard_run/scripts
./launch.sh
```

此处启动时若遇到问题详见 **使用说明.pdf**



### 10.2 依赖项

确保已安装以下依赖项：

#### 1. OpenCV

版本建议'4.10.0' 

可以通过以下命令安装 OpenCV：

```bash
sudo apt-get install python3-opencv
```

#### 2. YOLOv8  

您需要安装 ultralytics 包来使用 YOLOv8 模型： 

```bash
pip install ultralytics
```

#### 3. cv_bridge  

请确保已安装 cv_bridge 以便将 ROS 图像消息转换为 OpenCV 格式：

```bash
sudo apt-get install ros--cv-bridge
```

#### 4. gazebo_msgs  

请确保已安装 gazebo_msgs 以便与 Gazebo 进行通信：   

```bash
sudo apt-get install ros--gazebo-msgs
```

